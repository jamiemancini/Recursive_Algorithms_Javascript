//Fibonacci Recursive Function written in Javascript

function fibonacciRecursion (n) {
    if (n === 0) {total = total + 0
    };
    
    if (n === 1) {total = total + 1
    };

    if ( n > 1) { total = fibonaacciRecursion(n-1) + total

    };
}
